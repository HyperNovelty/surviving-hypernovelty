# Change Triage Worksheet

Sort changes into practical response buckets instead of treating everything as urgent.

## Use

```bash
python3 change_triage.py ../../examples/individual/change_triage_example.json
```

Buckets:

- `act_now`
- `monitor`
- `delegate`
- `ignore_for_now`
- `learn_later`
