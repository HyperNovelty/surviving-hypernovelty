# AI Delegation Contract Card

A small reviewable card for deciding what an AI/tool may do, must not do, and must prove.

## Use

```bash
python3 render_contract.py ../../examples/creator/ai_delegation_contract_example.json
```

The output is Markdown suitable for review packets, team docs, or iPublishOS-style provenance workflows.
